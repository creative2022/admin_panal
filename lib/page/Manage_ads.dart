import 'package:flutter/material.dart';



class Manage_AdsScreen extends StatelessWidget {
  const Manage_AdsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}