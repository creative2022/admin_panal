import 'package:flutter/material.dart';

class User_Manage_Screen extends StatelessWidget {
  const User_Manage_Screen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}